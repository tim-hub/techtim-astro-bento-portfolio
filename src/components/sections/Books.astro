---

import { getCollection } from "astro:content";


const books = await getCollection('books');

books.sort(
  (a, b) => {
    return b.data.readYear - a.data.readYear;
  }
);


---
<div class="h-full">
  <p class="text-sm font-light my-1">
    I like to read books as well, both fiction and non-fiction. Here are some of the books I have read.
  </p>
  <div class="h-full">
    <div class="mt-4 flex w-fit flex-col">
      <ul class="list-inside list-disc">
        {books.map(
          (book) => (
            <li>
              <a href={`/books/${book.slug}`} class="hover:font-bold underline">{book.data.title}</a>
              <span class="text-sm font-thin italic">- {book.data.author}</span>
            </li>
          )
        )}
      </ul>
    </div>
  </div>
</div>
